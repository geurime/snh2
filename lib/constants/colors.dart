import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF3F3F3);  // E1 Light Gray (25% lighter)
  static const Color card = Color(0xFFFFFFFF);        // White
  static const Color black = Color(0xFF30313A);       // Black Cat
  static const Color primary = Color(0xFFFFA987);     // Peach Sorbet
}
